{
  "name": "lagomorph",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:mobile": "vite build --mode mobile",
    "mobile": "vite build --mode mobile && cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^8.3.4",
    "@capacitor/core": "^8.3.4",
    "phaser": "^3.60.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.3.4",
    "playwright": "^1.60.0",
    "vite": "^5.0.0"
  }
}
